import { Dict } from 'i18n-js';
import { Metadata } from './typing';
export default class Storage {
    storage: Storage;
    constructor();
    loadingFromRedis(): string | null;
    app(): string | null;
    country(): string | null;
    brand(): string | null;
    setMetadata(metadata: Metadata): boolean;
    translations(): Dict;
    nonTeladocTranslations(): Dict;
    setNonTeladocTranslations(translations: Dict): void;
    removeNonTeladocTranslations(): void;
    timestamps(): Dict;
    storeTranslations(translations: Dict): void;
    storeTimestamps(timestamps: Dict): void;
    private setConfig;
    private storeString;
    private storeBoolean;
    private storeDict;
    private storeTranslationsDict;
    removeTimestampsForTimestampKeys(timestampKeys: string[]): void;
    removeTranslationsForTimestampKeys(timestampKeys: string[]): Dict;
    private removeTranslationsForDictionary;
    clearTranslations(): void;
}
